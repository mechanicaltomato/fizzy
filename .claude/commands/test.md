---
description: Run tests for the Fizzy application
---

Run the appropriate test suite for the Fizzy application.

Available test commands:
- `bin/rails test` - Run unit tests (fast)
- `bin/rails test:system` - Run system tests (browser-based)
- `bin/ci` - Run full CI suite (style, security, all tests)

Which tests would you like to run?
